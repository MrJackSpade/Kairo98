- Added a Fast forward emulator action that can be assigned to a controller or on-screen button. The game runs as fast as the device allows while the button is held.
- Fixed the built-in PC-98 font's half-width letters, which drew M and W like N and U. Letters now use a fixed-width PC-98 style that matches the Japanese text.
