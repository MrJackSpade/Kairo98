- Without an imported FONT.BMP, PC-98 text now uses a bundled 16-dot bitmap font built from the public-domain Shinonome fonts instead of glyphs rendered from the device's system fonts.
- An imported FONT.BMP still takes precedence. The Machine menu lists the fallback as "Built-in".
